{
  "name": "better-remote-control",
  "version": "0.1.1",
  "description": "Access your local terminal from a mobile browser via Cloudflare Tunnel",
  "type": "module",
  "bin": {
    "brc": "./dist/cli.js",
    "better-remote-control": "./dist/cli.js"
  },
  "scripts": {
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/darwin-*/spawn-helper 2>/dev/null || true",
    "build": "pnpm run build:client && pnpm run build:server",
    "build:server": "tsc",
    "build:client": "cd client && pnpm run build",
    "start": "node dist/cli.js",
    "dev:server": "tsc --watch",
    "dev:client": "cd client && pnpm run dev",
    "lint": "eslint src && cd client && pnpm exec eslint src",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky",
    "prepublishOnly": "pnpm run build"
  },
  "lint-staged": {
    "client/src/**/*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "src/**/*.ts": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,md,css}": [
      "prettier --write"
    ]
  },
  "keywords": [
    "terminal",
    "remote",
    "mobile",
    "cloudflare-tunnel",
    "xterm",
    "pty",
    "ssh-alternative",
    "web-terminal"
  ],
  "files": [
    "dist",
    "public"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/custardcream98/better-remote-control.git"
  },
  "homepage": "https://github.com/custardcream98/better-remote-control#readme",
  "bugs": {
    "url": "https://github.com/custardcream98/better-remote-control/issues"
  },
  "license": "MIT",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "express": "^5.1.0",
    "node-pty": "^1.0.0",
    "qrcode-terminal": "^0.12.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-perfectionist": "^5.7.0",
    "husky": "^9.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "prettier-plugin-tailwindcss": "^0.6.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.57.1"
  }
}

# i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add i18n support (en/ko) to the client UI using react-i18next, with browser language auto-detection and manual language switcher in settings.

**Architecture:** Install i18next + react-i18next + i18next-browser-languagedetector. Translation JSON files under `client/src/i18n/locales/`. Initialize in `main.tsx` before React renders. Replace all hardcoded Korean strings with `t()` calls. Add language toggle in existing settings dialog.

**Tech Stack:** i18next, react-i18next, i18next-browser-languagedetector

---

### Task 1: Install dependencies

**Files:**

- Modify: `client/package.json`

- [ ] **Step 1: Install packages**

```bash
cd client && pnpm add i18next react-i18next i18next-browser-languagedetector
```

- [ ] **Step 2: Verify installation**

```bash
cd client && pnpm ls i18next react-i18next i18next-browser-languagedetector
```

Expected: All three packages listed.

---

### Task 2: Create translation files and i18n config

**Files:**

- Create: `client/src/i18n/locales/en.json`
- Create: `client/src/i18n/locales/ko.json`
- Create: `client/src/i18n/index.ts`

- [ ] **Step 1: Create English translations**

Create `client/src/i18n/locales/en.json`:

```json
{
  "home": {
    "noSessions": "No sessions",
    "noSessionsDescription": "Start a new session to open a terminal",
    "newSession": "New Session"
  },
  "browse": {
    "openTerminal": "Open Terminal Here",
    "creating": "Creating\u2026",
    "sessionTimeout": "Session creation timed out"
  },
  "terminal": {
    "goHome": "Back to Home"
  },
  "config": {
    "connected": "Connected",
    "disconnected": "Disconnected",
    "reconnecting": "Reconnecting",
    "settings": "Settings"
  },
  "settings": {
    "title": "Settings",
    "autoCommand": "Auto Command",
    "autoCommandPlaceholder": "Command to auto-run in new terminals\u2026",
    "autoCommandDescription": "Automatically entered when a new terminal session starts",
    "language": "Language",
    "cancel": "Cancel",
    "save": "Save"
  },
  "session": {
    "close": "Close session"
  },
  "breadcrumb": {
    "path": "Path"
  },
  "directory": {
    "retry": "Retry",
    "noSubdirectories": "No subdirectories"
  },
  "upload": {
    "maxSize": "10MB max",
    "failed": "Upload failed"
  }
}
```

- [ ] **Step 2: Create Korean translations**

Create `client/src/i18n/locales/ko.json`:

```json
{
  "home": {
    "noSessions": "세션 없음",
    "noSessionsDescription": "새 세션을 시작해서 터미널을 열어보세요",
    "newSession": "새 세션 시작"
  },
  "browse": {
    "openTerminal": "여기서 터미널 열기",
    "creating": "생성 중\u2026",
    "sessionTimeout": "세션 생성 시간 초과"
  },
  "terminal": {
    "goHome": "홈으로 돌아가기"
  },
  "config": {
    "connected": "연결됨",
    "disconnected": "연결 끊김",
    "reconnecting": "재연결 중",
    "settings": "설정"
  },
  "settings": {
    "title": "설정",
    "autoCommand": "자동 명령어",
    "autoCommandPlaceholder": "새 터미널에서 자동 실행할 명령어\u2026",
    "autoCommandDescription": "새 터미널 세션 시작 시 자동으로 입력됩니다",
    "language": "언어",
    "cancel": "취소",
    "save": "저장"
  },
  "session": {
    "close": "세션 닫기"
  },
  "breadcrumb": {
    "path": "경로"
  },
  "directory": {
    "retry": "재시도",
    "noSubdirectories": "하위 디렉토리 없음"
  },
  "upload": {
    "maxSize": "10MB 이하만",
    "failed": "업로드 실패"
  }
}
```

- [ ] **Step 3: Create i18n config**

Create `client/src/i18n/index.ts`:

```typescript
import i18n from "i18next";
import LanguageDetector from "i18next-browser-languagedetector";
import { initReactI18next } from "react-i18next";

import en from "./locales/en.json";
import ko from "./locales/ko.json";

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      en: { translation: en },
      ko: { translation: ko },
    },
    fallbackLng: "en",
    interpolation: { escapeValue: false },
    detection: {
      order: ["localStorage", "navigator"],
      caches: ["localStorage"],
      lookupLocalStorage: "brc_lang",
    },
  });

export default i18n;
```

- [ ] **Step 4: Import i18n in main.tsx**

Modify `client/src/main.tsx` — add `import "@/i18n";` before the React render:

```typescript
import { RouterProvider } from "@tanstack/react-router";
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

import "@/i18n";
import { router } from "@/router";
import "@/index.css";

createRoot(document.getElementById("root")!).render(
  <StrictMode>
    <RouterProvider router={router} />
  </StrictMode>,
);
```

- [ ] **Step 5: Commit**

```bash
git add client/src/i18n/ client/src/main.tsx client/package.json client/pnpm-lock.yaml
git commit -m "feat(i18n): add i18next setup with en/ko translations"
```

---

### Task 3: Replace hardcoded strings in route pages

**Files:**

- Modify: `client/src/routes/index.tsx`
- Modify: `client/src/routes/browse.tsx`
- Modify: `client/src/routes/terminal.$sessionId.tsx`

- [ ] **Step 1: Update index.tsx (HomePage)**

Add `import { useTranslation } from "react-i18next";` and `const { t } = useTranslation();` inside `HomePage`.

Replace:

- `"세션 없음"` → `{t("home.noSessions")}`
- `"새 세션을 시작해서 터미널을 열어보세요"` → `{t("home.noSessionsDescription")}`
- `"새 세션 시작"` (inside Link) → `{t("home.newSession")}`

- [ ] **Step 2: Update browse.tsx (BrowsePage)**

Add `useTranslation` hook.

Replace:

- `"세션 생성 시간 초과"` → `t("browse.sessionTimeout")`
- `creating ? "생성 중\u2026" : "여기서 터미널 열기"` → `creating ? t("browse.creating") : t("browse.openTerminal")`

- [ ] **Step 3: Update terminal.$sessionId.tsx (TerminalPage)**

Add `useTranslation` hook.

Replace:

- `"홈으로 돌아가기"` → `{t("terminal.goHome")}`

- [ ] **Step 4: Commit**

```bash
git add client/src/routes/
git commit -m "feat(i18n): replace hardcoded strings in route pages"
```

---

### Task 4: Replace hardcoded strings in components

**Files:**

- Modify: `client/src/components/config-bar.tsx`
- Modify: `client/src/components/settings-dialog.tsx`
- Modify: `client/src/components/session-card.tsx`
- Modify: `client/src/components/breadcrumb-nav.tsx`
- Modify: `client/src/components/directory-list.tsx`
- Modify: `client/src/components/quick-keys.tsx`

- [ ] **Step 1: Update config-bar.tsx**

Add `useTranslation` hook.

Replace:

- `"연결됨"` → `t("config.connected")`
- `"연결 끊김"` → `t("config.disconnected")`
- `"재연결 중"` → `t("config.reconnecting")`
- `aria-label="설정"` → `aria-label={t("config.settings")}`

- [ ] **Step 2: Update settings-dialog.tsx**

Add `useTranslation` hook to `SettingsDialogContent`.

Replace:

- `<DialogTitle>Settings</DialogTitle>` → `<DialogTitle>{t("settings.title")}</DialogTitle>`
- `<Label>Auto Command</Label>` → `<Label>{t("settings.autoCommand")}</Label>`
- `placeholder="새 터미널에서 자동 실행할 명령어…"` → `placeholder={t("settings.autoCommandPlaceholder")}`
- `"새 터미널 세션 시작 시 자동으로 입력됩니다"` → `{t("settings.autoCommandDescription")}`
- `Cancel` → `{t("settings.cancel")}`
- `Save` → `{t("settings.save")}`

Add language selector (dropdown or toggle):

```tsx
<div className="grid gap-1.5">
  <Label htmlFor="lang">{t("settings.language")}</Label>
  <select
    id="lang"
    value={i18n.language.startsWith("ko") ? "ko" : "en"}
    onChange={(e) => i18n.changeLanguage(e.target.value)}
    className="w-full rounded-md border border-[var(--border)] bg-[var(--background)] px-3 py-2 text-sm text-[var(--foreground)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[var(--ring)]"
  >
    <option value="en">English</option>
    <option value="ko">한국어</option>
  </select>
</div>
```

- [ ] **Step 3: Update session-card.tsx**

Add `useTranslation` hook.

Replace:

- `aria-label="세션 닫기"` → `aria-label={t("session.close")}`

- [ ] **Step 4: Update breadcrumb-nav.tsx**

Add `useTranslation` hook.

Replace:

- `aria-label="경로"` → `aria-label={t("breadcrumb.path")}`

- [ ] **Step 5: Update directory-list.tsx**

Add `useTranslation` hook.

Replace:

- `"재시도"` → `{t("directory.retry")}`
- `"하위 디렉토리 없음"` → `{t("directory.noSubdirectories")}`

- [ ] **Step 6: Update quick-keys.tsx**

Add `useTranslation` hook.

Replace:

- `alert("10MB 이하만")` → `alert(t("upload.maxSize"))`
- `alert("업로드 실패")` → `alert(t("upload.failed"))`

- [ ] **Step 7: Commit**

```bash
git add client/src/components/
git commit -m "feat(i18n): replace hardcoded strings in all components"
```

---

### Task 5: Update Storybook for i18n

**Files:**

- Modify: `client/.storybook/preview.ts`

- [ ] **Step 1: Add i18n import to Storybook preview**

Modify `client/.storybook/preview.ts` to import i18n config:

```typescript
import type { Preview } from "@storybook/react";
import "../src/i18n";
import "../src/index.css";

const preview: Preview = {
  parameters: {
    viewport: {
      defaultViewport: "mobile1",
    },
    backgrounds: {
      default: "dark",
      values: [{ name: "dark", value: "#121629" }],
    },
    a11y: {
      test: "todo",
    },
  },
};

export default preview;
```

- [ ] **Step 2: Update stories with hardcoded Korean**

Update `home-page.stories.tsx`, `browse-page.stories.tsx`, and `directory-list.stories.tsx` to use `t()` or replace hardcoded Korean with English equivalents (since stories import the i18n config and will use the default English fallback).

For `home-page.stories.tsx` and `browse-page.stories.tsx`, these are "page layout reproductions" that duplicate route code. Replace Korean strings with their English translation equivalents since i18n `t()` calls will work via the imported config.

- [ ] **Step 3: Verify Storybook runs**

```bash
cd client && pnpm run storybook
```

Verify stories render with English text by default.

- [ ] **Step 4: Commit**

```bash
git add client/.storybook/ client/src/components/__stories__/
git commit -m "feat(i18n): update storybook preview and stories for i18n"
```

---

### Task 6: Re-capture screenshots with English UI

**Files:**

- Update: `docs/screenshots/*.png`

- [ ] **Step 1: Start Storybook**

```bash
cd client && pnpm run storybook &
```

- [ ] **Step 2: Capture screenshots**

Run the Playwright capture script (or create one) to screenshot key stories with English locale. Screenshots: `home.png`, `browse.png`, `quick-keys.png`, `quick-keys-ctrl.png`.

- [ ] **Step 3: Stop Storybook and commit**

```bash
git add docs/screenshots/
git commit -m "docs: update screenshots with English UI"
```

---

### Task 7: Verify full build

- [ ] **Step 1: Type check**

```bash
cd client && pnpm exec tsc -b
```

Expected: No errors.

- [ ] **Step 2: Lint**

```bash
cd /Users/shiwoo/dev/better-remote-control && pnpm run lint
```

Expected: No errors.

- [ ] **Step 3: Full build**

```bash
cd /Users/shiwoo/dev/better-remote-control && pnpm run build
```

Expected: Build succeeds.
